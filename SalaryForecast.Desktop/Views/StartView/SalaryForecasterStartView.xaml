<Window x:Class="SalaryForecast.Desktop.Views.StartView.SalaryForecasterStartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:startViewModel="clr-namespace:SalaryForecast.Core.ViewModels.StartViewModel;assembly=SalaryForecast.Core"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance startViewModel:SalaryForecasterStartViewModel}"
        Title="{Binding DisplayName}"
        WindowStartupLocation="CenterScreen"
        SizeToContent="WidthAndHeight"
        MinHeight="480"
        MinWidth="640">
    <Grid Margin="11" IsSharedSizeScope="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Margin="0,0,3,0" Text="До ближайшей зарплаты осталось"/>
            <TextBlock Margin="0,0,3,0" Text="{Binding DaysString}"/>
        </StackPanel>
        <TextBlock Grid.Column="0" Grid.Row="1" Text="Предыдущий год" HorizontalAlignment="Center"/>
        <TextBlock Grid.Column="1" Grid.Row="1" Text="Текущий год" HorizontalAlignment="Center"/>
        <ListView ItemsSource="{Binding SalariesPast}" Grid.Column="0" Grid.Row="2">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Focusable" Value="false"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*" SharedSizeGroup="A"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*" SharedSizeGroup="A"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" Margin="0,0,25,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding SalaryPart, StringFormat={}{0:F2}}" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Column="2" Text="рублей" Margin="6,0,25,0"/>
                        <TextBlock Grid.Column="3" Text="{Binding SalaryPercent, StringFormat={}{0:F2}}" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Column="4" Text="%" Margin="6,0,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ListView ItemsSource="{Binding Salaries}" Grid.Column="1" Grid.Row="2" SelectedItem="{Binding SelectedSalary, Mode=OneWay}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Focusable" Value="false"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*" SharedSizeGroup="A"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*" SharedSizeGroup="A"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" Margin="0,0,25,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding SalaryPart, StringFormat={}{0:F2}}" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Column="2" Text="рублей" Margin="6,0,25,0"/>
                        <TextBlock Grid.Column="3" Text="{Binding SalaryPercent, StringFormat={}{0:F2}}" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Column="4" Text="%" Margin="6,0,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
